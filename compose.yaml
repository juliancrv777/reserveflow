services:
  api:
    build: .
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - reserveflow-data:/app/data
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
volumes:
  reserveflow-data:
